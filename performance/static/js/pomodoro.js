import { 
        hours,
        minutes,
        seconds,
        displayHours,
        displayMinutes,
        displaySeconds,
        buttonStart,
        buttonStop,
        buttonReset,
        timerValue,
        Interval
    } from './another-file.js';

timerValue.value = '0:0:0';

const config_values = ['intervals', 'workDuration', 'workDurationHrOrMin', 'breakDuration' ,'breakDurationHrOrMin'];

function startPomodoro(numberOfIntervals, durationOfInterval, durationOfIntervalTime,
     durationOfBreak, durationBreakTime){
  
}